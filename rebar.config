%%% -*- mode: erlang -*-

{deps, [
        {parse_trans, ".*", {git, "https://github.com/uwiger/parse_trans.git", "2.6"}},
        {lager, ".*", {git, "https://github.com/basho/lager.git", "2.0.0"}}
       ]
}.

{cover_enabled, true}.

{clean_files, ["test/*.beam"]}.

{erl_opts, [{parse_transform, lager_transform}]}.
